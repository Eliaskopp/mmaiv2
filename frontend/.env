# VITE_API_URL is intentionally omitted — api-client.ts uses '/api' which
# routes through the Vite dev proxy in development. For production, configure
# the reverse proxy (nginx) to route /api to the backend instead.
